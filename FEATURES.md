# 기능 명세 (두루런)

## 서비스 개요

해파랑길을 중심으로 한 한국 해안 트레일 러닝 서비스.
두루누비 공식 코스(DRNB)와 유저가 직접 만든 커스텀 코스를 탐색하고,
완주 기록을 남기고 리뷰를 공유할 수 있는 웹 서비스.

---

## 역할 (user_role)

| 역할 | 설명 |
|------|------|
| `USER` | 일반 유저. 코스 탐색, 러닝 기록, 리뷰 작성, 커스텀 코스 생성 가능 |
| `ADMIN` | 관리자. 편의시설 관리, 리뷰 삭제, 대시보드 통계 조회 가능 |

---

## 기능 목록

### 1. 회원 (인증)

| 기능 | 설명 |
|------|------|
| 소셜 로그인 | Google / Kakao / Naver OAuth 2.0 연동. 이메일/비밀번호 로그인 없음 |
| 로그아웃 | Access Token 블랙리스트 등록 + Refresh Token 삭제 |
| JWT 발급 및 검증 | Access Token + Refresh Token 기반 인증 |
| 토큰 재발급 | Refresh Token으로 Access Token 재발급 (토큰 로테이션) |
| 회원가입 | 소셜 로그인 최초 시 닉네임, 거주지 입력 |
| 회원 탈퇴 | 개인정보 익명화 처리. `deleted_at` 기록. `social_accounts` Hard Delete. **단일 트랜잭션 처리** |

**비즈니스 로직**
- 소셜 로그인 시 `social_accounts`에 `provider_type` + `provider_uid` 저장
- 같은 소셜 계정으로 재가입 시 기존 계정으로 로그인 처리
- `ADMIN` 계정은 회원가입 API로 생성 불가. 일반 소셜 로그인으로 가입 후 DB에서 `user_role`을 수동으로 `ADMIN`으로 승격 (2026-09-09 팀 결정 — 관리자 인원이 적어 별도 가입/승격 API·스크립트를 둘 필요가 없고, 애초에 그런 API가 없어야 권한 상승 공격 경로도 안 생김)
- OAuth state는 Redis(`oauth:state:{state}`, TTL 5분)에 저장하여 콜백 시 검증 (CSRF 방지)

**토큰 전략 (JWT)**

| 토큰 | 만료 | 저장 위치 | 전송 방식 |
|------|------|----------|----------|
| Access Token | 30분 | 클라이언트 localStorage | 응답 body(JSON) + 요청 시 `Authorization: Bearer` 헤더 |
| Refresh Token | 14일 | httpOnly 쿠키 (`/api/v1/auth/refresh` 경로 한정) | `Set-Cookie` |

- **세션 정책**: 유저당 Refresh Token 1개 (Redis `refresh:{user_id}`). 재발급 시 기존 토큰을 새 토큰으로 교체(로테이션)
- **재발급 검증**: 클라이언트가 보낸 Refresh의 jti가 Redis 값과 일치할 때만 새 Access + 새 Refresh 발급 (GET-then-SET을 Lua로 원자적 처리). 불일치 시 401로 재발급 거부 — 단, Redis 키는 삭제하지 않음 (동시 재발급 요청 중 하나가 먼저 로테이션에 성공한 뒤, 뒤늦게 도착한 옛 토큰 요청이 방금 발급된 정상 세션까지 강제 로그아웃시키는 경쟁 조건을 막기 위함. 2026-08-12 `e40682a`에서 변경)
- **로그인/재발급 응답**: Access Token은 응답 body로, Refresh Token은 httpOnly 쿠키(Set-Cookie)로 전달
- **쿠키 환경 분기**: `samesite=lax`는 로컬/프로덕션 공통(같은 도메인이라 충분), `secure`만 환경 분기(로컬 `False`, 프로덕션 `True`). CORS는 `allow_credentials=True`, 프론트는 `credentials: 'include'` 필수
- **로그아웃**: Access를 `blacklist:{access_jti}`에 잔여 만료시간만큼 등록, Refresh 쿠키 삭제 + Redis `refresh:{user_id}` 삭제

**탈퇴 처리 상세** (단일 트랜잭션으로 일괄 처리)
- `users`: `name`, `nickname`, `profile_image_url`, `location` → NULL 처리, `deleted_at` 기록 (Soft Delete)
- `social_accounts`: Hard Delete (개인 식별 정보 즉시 파기)
- `records`: 서비스 레이어에서 `user_id` 명시적 NULL 업데이트 (기록 보존, 통계 활용)
- `reviews`: 서비스 레이어에서 `user_id` 명시적 NULL 업데이트 (리뷰 내용/평점 보존, 통계 활용). 화면에서는 `WHERE user_id IS NOT NULL`으로 제외
- `courses`(커스텀 코스만 해당): **다른 유저와 얽혀있는지에 따라 분기** (2026-09-10 팀 결정)
  - 이 코스에 다른 유저(이미 탈퇴해 `user_id`가 NULL인 유저 포함)의 `records`/`reviews`가 하나라도 있으면(엮여있음) → 지금처럼 `created_by`만 NULL 처리, 코스는 서비스에 계속 노출 (다른 사람의 기록/통계를 훼손할 수 없으므로)
  - 이 코스에 본인 것뿐이거나 아무 기록도 없으면(안 엮여있음) → 코스 row 자체를 **완전 삭제** (Hard Delete). 주인 없이 영원히 남는 찌꺼기 데이터를 막기 위함. 이 경우 그 코스에 달린 본인의 `records`/`reviews`, 경유지(`course_waypoints`), 이미지(`course_images`, R2 파일 포함), 편의시설 매핑(`course_facility`)도 함께 삭제됨
  - 강제 탈퇴(관리자)에도 동일하게 적용
- 위 처리는 하나의 트랜잭션으로 묶어 중간 실패 시 전체 롤백 (social만 삭제되고 user가 남는 불일치 방지)

---

### 2. 프로필 (마이페이지)

| 기능 | 설명 |
|------|------|
| 내 정보 조회 / 수정 | 닉네임, 거주지, 프로필 이미지 수정 가능 |
| 프로필 이미지 업로드 | Cloudflare R2 업로드 후 URL을 `users.profile_image_url`에 저장 |
| 내 러닝 기록 조회 | 완주한 코스 목록, 총 누적 거리, 총 완주 횟수 |
| 내 리뷰 조회 | 작성한 리뷰 목록 |
| 내 커스텀 코스 조회 | 내가 만든 커스텀 코스 목록 |

---

### 3. 코스

#### 3-1. DRNB 코스 (두루누비 공식 코스)

| 기능 | 설명 |
|------|------|
| 코스 목록 조회 | 필터/검색 적용하여 DRNB 코스 목록 조회 |
| 코스 상세 조회 | 코스 상세정보 조회. 시드 스크립트로 저장된 DB 정보만 사용 (배치 갱신) |
| 코스 필터 | 지역(`sigun`), 난이도(`difficulty`), 거리(`distance_min`/`distance_max`) - 프론트 필터 UI 기준. `brd_div`(루트/구간)는 백엔드 쿼리 파라미터만 존재하고 프론트 필터 UI는 없음(뱃지 표시 용도로만 사용). `estimated_time`(소요시간) 필터는 미구현 |
| 편의시설 표시 | 코스 주변 편의시설 지도에 표시 (반경 기반 자동 매칭, 섹션 6 참고) |

**두루누비 API 연동**
- 코스 목록/상세에 필요한 정보(`course_name`, `difficulty`, `estimated_time`, `sigun`, `brd_div`, GPX 좌표, 거리, 설명)를 시드 스크립트가 배치로 조회해 DB에 저장. 요청 시점에 두루누비 API를 실시간 호출하지 않음 (API 장애와 무관하게 조회 동작 보장)
- 데이터 최신화 주기 = 시드 스크립트 재실행 주기
- 모든 필터는 DB 값 기준으로 적용
- **현재 수집 범위**: 트레일 종류 무관하게 두루누비 코스 전체를 가져와서, 그중 `sigun`이 "강원"으로 시작하는 것만 DB에 저장 (`app/scripts/seed_courses.py`의 `_TARGET_REGION`). 강원 코스는 해파랑길 + DMZ 평화의 길 2개 트레일만 존재.
- **서비스 지역 확장 결정 (2026-08-27)**: 관광콘텐츠 공모전이기 때문에, 서비스 대상 지역을 "강원 해안 6개 시군(삼척~고성)"이 아니라 **강원도 전체**로 넓히기로 팀 결정.
- **DMZ 평화의 길 강원 전체 확장 (2026-09-09)**: 기존엔 `_TRAIL_NAME="해파랑길"` 서버단 필터 때문에 DMZ 평화의 길이 전혀 수집되지 않았음. 이 필터를 제거하고 강원 지역 필터(`_TARGET_REGION`)만으로 전체 재수집. 강원 DMZ 평화의 길 전체 범위는 16~34번(우회로 포함 23개)이며, 이 중 다음 12개는 `_EXCLUDED_COURSE_IDS`로 서비스에서 제외:
  - **16, 26번**: 전체예약(민통선 출입 필요) 코스라 제외. 대신 예약이 필요 없는 대체 우회로 **16-1, 26-1**을 서비스에 포함 (26-1은 API 응답에 포함돼 있어 자동 수집, 16-1은 API 미제공이라 수동 데이터로 추가)
  - **19번**: 원문에 "현재 낙석위험으로 잠정 출입통제 중" + "산불조심기간·동절기(11월~5월) 통제"로 명시돼 있어 상시 도보가 불가능한 상태라 제외. 대체 우회로 **19-1**을 수동 데이터로 추가
  - **29~34번(우회로 포함: 31-1, 32-1, 33-1)**: 군사접경지역 특성상 코스마다(우회로 포함) 예약/민간인통제구역 관련 안내가 일관성 없이 박혀있어(코스명에 "(예약노선)" 표기가 없어도 본문에만 예약 문구가 있는 경우 다수 확인) API 필드만으로 안전하게 구분할 수 없다고 판단, 이 번호대 전체를 서비스에서 제외
  - "러닝 시작" 완주 기록 기능이 "지금 바로 전 구간 도보 가능"을 전제하기 때문에, 부분적으로만 예약이 필요한 코스(예: 32번)도 전체예약과 동일하게 제외 대상으로 취급
- **DMZ 코스 신분증 안내**: 민통선 인접 구간이 잦아 코스명에 "DMZ"가 포함된 코스는 상세페이지에 "신분증 소지 및 야간통행 주의" 안내 문구를 조건부로 노출
- **일부 코스 제외 안내**: 위 12개 제외 코스에 대해 코스 목록 페이지 footer에 안내 문구 + 두루누비 공식 홈페이지(durunubi.kr) 링크 노출
- **API 데이터 누락 이슈**: 한국관광공사 대외 개방 데이터 운영팀 공식 확인(2026-08-24) — 해파랑길 29·32·36·37·38·43번 코스는 API 버그가 아니라 **두루누비 정보 서비스 자체에서 조회 불가능한 데이터**. `app/scripts/manual_courses.py`의 하드코딩된 수동 데이터로 영구 보완 (API 정상화를 기다리는 임시방편이 아님). DMZ 평화의 길 17·27·28번(API 누락)과 16-1·19-1번(우회로, API 미제공)도 2026-09-09에 같은 방식으로 수동 보완 — 단, 이쪽은 아직 운영 기관 공식 확인 전이라 API가 나중에 정상 반환하면 `_merge_manual_courses`가 자동으로 API 데이터를 우선함

#### 3-2. 커스텀 코스

| 기능 | 설명 |
|------|------|
| 커스텀 코스 등록 | 유저가 직접 코스 생성. 코스명, 거리, 난이도, 소요시간, 설명, 경유지 좌표, 이미지 입력. 경유지는 지도를 클릭해서 순서대로 지정 (GPS는 지도 초기 위치를 잡아주는 편의 기능일 뿐, 필수 아님) |
| 커스텀 코스 이미지 업로드 | Cloudflare R2 업로드. 최대 3개 / 장당 5MB (`COURSE_IMAGE_MAX_COUNT=3`, `COURSE_IMAGE_MAX_SIZE_MB=5`) |
| 커스텀 코스 수정 | 본인만 수정 가능 |
| 커스텀 코스 삭제 | 본인만 삭제 가능. `is_active = false` 처리. 신규 탐색/러닝 시작에서만 제외. 기존 유저 마이페이지 기록 및 관리자 대시보드 통계에는 계속 포함 |
| 커스텀 코스 목록 조회 | 전체 유저가 검색 가능. 난이도, 거리 필터. offset 페이지네이션 |
| 커스텀 코스 상세 조회 | 경유지 좌표(`course_waypoints`) 기반으로 지도에 경로 표시. 코스 주변 편의시설 함께 표시 |

**비즈니스 로직**
- 경유지 좌표는 `course_waypoints`에 `sequence` 순서대로 저장
- 지도에서 좌표를 순서대로 직선(폴리라인)으로 이어 참고 경로를 그림 — 실제 도로/트레일을 따라가는 경로가 아니라 유저가 찍은 경유지끼리 그냥 직선으로 연결한 것 (경유지를 촘촘히 찍을수록 실제 경로에 가까워짐)
- `start_lat/lng`는 `course_waypoints` sequence=0 좌표, `end_lat/lng`는 마지막 sequence 좌표로 자동 저장
- 경유지 좌표 때문에 GPS 필수로 생각했었는데, 경유지 좌표는 지도 클릭 방식으로 구현 가능
- **GPS는 선택**: 커스텀 코스 생성은 위치 권한을 거부하거나 브라우저가 지원하지 않아도 진행 가능. GPS는 지도를 열 때 유저 위치가 강원 지역 근처면 그 위치로, 아니면 강원 기본 위치로 초기 중심을 잡아주는 용도로만 사용 (지역이 강원도로 고정된 서비스에서 GPS를 강제하면 오히려 서비스 지역 밖 유저(관광 계획 세우는 외지인 등)의 코스 생성을 막는 역효과가 있다고 판단. 지역 관광 공모전이므로 외지인의 접근성이 중요하다고 생각.)
- 탈퇴한 유저의 커스텀 코스는 `created_by = NULL` 처리 후 서비스에 계속 노출
- **지역 제한 (2026-08-27 팀 결정)**: 커스텀 코스는 **강원도 전체**에서만 생성 가능 (관광콘텐츠 공모전 취지 고려).
  - 백엔드(`CourseWaypointCreate`, 최종 기준): 강원도 실제 경계(폴리곤)로 판정. [국가데이터처 SGIS 행정구역 통계 및 경계_20250630](https://www.data.go.kr/data/15129688/fileData.do)(2025년 2분기 기준 시도 경계, 이용허락범위 제한 없음)에서 강원도만 추출해 `app/domain/course/gangwon_boundary/gangwon_boundary.geojson`에 저장해두고(추출 스크립트: `app/scripts/extract_gangwon_boundary.py`), `shapely`로 point-in-polygon 판정. 코드리뷰에서 "사각형 박스는 강원도가 아닌 좌표(예: 37.5, 127.5)도 통과시킨다"는 지적을 받아 2026-09-03 폴리곤 방식으로 교체함 — 철원군을 위해 박스를 2개로 나눴던 것도 실제 경계엔 자연히 포함돼 필요 없어짐. 원본 shapefile은 EPSG:5179(미터) 좌표계라 WGS84로 재투영, 부속 도서(무인도 등 904개 조각)는 커스텀 코스 waypoint 용도로 불필요해 본토만 사용
  - 프론트(지도 클릭 시): `GET /v1/courses/gangwon-boundary`로 백엔드와 같은 geojson을 받아 클릭 시점에 바로 point-in-polygon 판정(`frontend/src/lib/pointInPolygon.js`, 의존성 추가 없이 ray-casting 직접 구현 — 이미 있는 백엔드 테스트 좌표들 및 경계선/꼭짓점 케이스로 shapely 판정과 검증한 결과, 실사용 범위(내부/외부 좌표)에서는 그대로 일치하고 외곽 경계 "꼭짓점 그 자체"와 좌표가 완전히 일치하는 극히 드문 경우만 다를 수 있음(`frontend/scripts/verify-point-in-polygon.js`의 `KNOWN_DIVERGENCE_CASES`) — 최종 판정은 항상 백엔드가 하므로 실사용 영향 없음). 처음엔 대략적인 사각형 박스만 썼지만, 저장 시점에야 거부당하는 UX 갭이 있어 2026-09-03 폴리곤 방식으로 교체 — 사각형 박스(`_isNearGangwonBox`)는 경계 데이터 로드 전/실패 시에만 쓰는 폴백으로 남김.
- **지역 태깅 및 필터 (2026-09-09)**: DRNB가 강원 전체로 확장되면서 커스텀 코스도 지역 필터가 필요해짐. 커스텀 코스는 DRNB처럼 시군 정보가 API로 안 내려오고 좌표만 있어서, 시작/종료 좌표 각각으로 시군을 판별해 `sigun`(시작)/`end_sigun`(종료) 컬럼에 저장.
  - **판별 방식**: 카카오 등 외부 지오코딩 API 대신, 강원도 18개 시군구 경계 폴리곤으로 로컬 point-in-polygon 판정 (`find_sigungu()`, `app/domain/course/schemas.py`). 외부 API 호출이 아예 없어 쿼터/네트워크 장애 리스크가 없음. 시군구 경계 데이터는 강원도 전체 경계와 같은 출처(SGIS 행정구역 통계 및 경계_20250630)에서 추출 (`app/scripts/extract_gangwon_sigungu_boundary.py` → `gangwon_sigungu_boundary.geojson`)
  - 코스 생성 시 1회, 수정 시 waypoints가 실제로 바뀔 때만 재계산 (제목/설명만 바뀌는 수정은 재계산 안 함)
  - 목록 필터(`GET /v1/courses/custom?sigun=`)는 시작 또는 종료 시군 중 하나만 일치해도 매칭 — 시군 경계를 걸치는 코스도 양쪽 지역 검색에서 찾을 수 있음
  - 드롭다운 옵션은 DRNB처럼 강원 18개 시군 전체를 고정 노출하지 않고, `GET /v1/courses/custom/sigun-options`로 실제 코스가 존재하는 시군만 동적으로 조회 (유저가 계속 새 지역에 코스를 만들 수 있어 하드코딩하면 금방 낡음)
  - 카드 표시: 시작=종료 시군이면 하나만("춘천시"), 다르면 화살표로 구분("춘천시 → 홍천군")

#### 3-3. 코스 상세 페이지 구성 (2026-08-27 회의 결정)

코스 기본정보 아래로 아래 순서로 배치 (구현 예정, 담당: 코스 담당):

```
코스 기본정보 (구현됨)
  ↓
지도 (경로 + 편의시설 마커) — 카카오맵 컴포넌트
  ↓
[AI 버튼("코스 날씨·안전 브리핑")은 코스 이름 옆 상단에 위치]
  ↓
리뷰란 (리뷰 도메인 - 구현됨, 프론트 연동만 남음)
  ↓
주변 관광지 추천 카드 (가로 스크롤)
```

#### 3-4. AI 코스 날씨·안전 브리핑

| 기능 | 설명 |
|------|------|
| AI 날씨·안전 브리핑 | 코스 이름 옆 "코스 날씨·안전 브리핑" 버튼 클릭 시, 날씨/특보 정보를 AI가 다듬은 문구로 모달 표시 |

**연동 API**
- 기상청 단기예보 조회서비스 (`getVilageFcst`) — 좌표 기준 날씨. 위경도 → 기상청 격자좌표(nx/ny) 변환 필요
- 기상청 기상특보 조회서비스 — 안전(강풍/호우/풍랑/폭염/한파 등 특보) 정보. 특보구역코드 기준이라 좌표 → 행정구역(시군) 변환 필요
- AI 문구 생성: Gemini 2.5 Flash-Lite (리뷰 AI 요약과 동일 모델, 클라이언트 재사용)

**비즈니스 로직**
- 코스가 시군 2개에 걸치는 경우(커스텀 코스에서 가능, DRNB는 `sigun` 컬럼이 코스당 1개라 발생 안 함): 시작/종료 좌표의 중간점을 대표 좌표로 사용해 날씨·특보 조회
- **캐싱 필수**: 단기예보 갱신 주기(3시간)에 맞춰 지역 단위로 캐싱 — 매 클릭마다 기상청 API 2개 + AI 호출을 라이브로 하지 않음. 캐시 키는 코스가 아니라 **지역(시군/격자)** 단위 (DRNB는 `sigun` 그대로, 커스텀은 좌표→행정구역 변환 결과 또는 격자좌표)
- 캐시에는 원본 기상 데이터가 아니라 AI가 다듬은 최종 문구까지 저장 (캐시 히트 시 AI 재호출 없이 즉시 응답)
- 외부 의존성 3개(기상청 2개 + AI)가 얽혀있어 하나 실패해도 전체가 죽지 않게 방어적으로 처리 필요 (예: 특보 실패 시 날씨만이라도 표시)
- v2나 추후 디벨롭된다면, 이 모달에 자외선 내용도 포함되면 더 좋을 것 같다는 실제 러너 의견 있었음(현재 api에는 없는 내용).

#### 3-5. 주변 관광지 추천

| 기능 | 설명 |
|------|------|
| 관광지 추천 카드 | 코스 시작점/종료점 근처 관광지를 카드형으로 가로 스크롤 표시 |

**연동 API**
- 한국관광공사 국문 관광정보 서비스 - 위치기반 관광정보 조회 Open API

**비즈니스 로직**
- **캐싱 없음 (2026-09 결정)**: 공모전 규정상 이 API는 로컬 저장/캐싱 없이 실시간 호출 방식으로 활용하도록 권고돼, 코스 상세 조회마다(시작/종료 좌표 각각) 매번 관광공사 API를 새로 호출한다 (`attraction_service.py`). AI 브리핑(3-4)의 지역 단위 TTL 캐싱과는 다른 방식이니 혼동 주의
- 캐싱이 없어 모든 조회가 실제 외부 호출이라 두 단계로 보호: ① IP당 시간당 요청 수 제한(`NEARBY_ATTRACTIONS_RATE_LIMIT_MAX_REQUESTS`), ② 이 API 자체의 일일 트래픽 한도(팀 확인: 1,000회/일)를 지키기 위한 서버 전체 기준 전역 카운터(`NEARBY_ATTRACTIONS_DAILY_QUOTA_MAX_CALLS`, 여유분 남겨 900으로 설정) - IP별 제한만으로는 서로 다른 다수 IP가 나눠서 하루 쿼터 자체를 소진하는 걸 못 막아서 추가함(2026-09-15 코드리뷰 지적). 전역 쿼터를 다 쓰면 남은 좌표는 호출하지 않고 카드 없이 조용히 넘어감(에러 노출 X)
- 시작/종료점 중 하나라도 API 호출이 실패해도 에러를 노출하지 않고 조용히 스킵, 성공한 지점 결과만 표시 (관광지 카드는 부가 기능이라는 원칙)

---

### 4. 러닝 기록

| 기능 | 설명 |
|------|------|
| 기록 시작 | 시작 버튼 누르면 `started_at` + 유저 GPS 시작 좌표 기록. **위치 권한 필수** |
| 기록 종료 | 종료 버튼 누르면 `ended_at` + 유저 GPS 종료 좌표 기록. `duration_seconds`, `pace`, `is_completed` 자동 계산 |
| 기록 조회 | 내 러닝 기록 목록 조회 (최신순, offset 페이지네이션 `?page=1&size=20`). 같은 코스 여러 번 달린 히스토리 모두 표시 |
| 기록 삭제 | 본인만 삭제 가능 |
| 기록 일시정지 | 러닝 중 일시정지. `paused_at` 기록 |
| 기록 재시작 | 일시정지 해제, `paused_at` NULL로 초기화 |
| 완주 인증 | GPS 검증 통과 시 `is_completed = true` 자동 처리. 완주 시 해당 코스 리뷰 작성 가능 |
| 페이스 자동 계산 | 평균 페이스 = `duration_seconds / 코스거리(km)` (초/km). 종료 시 자동 계산하여 저장 |

**비즈니스 로직**
- 시작 버튼 → `started_at` + `user_start_lat/lng` 기록
- 종료 버튼 → `ended_at` + `user_end_lat/lng` 기록 → `duration_seconds`, `pace`, `is_completed` 자동 계산
- 일시정지 버튼 → `paused_at` 현재 시각 기록. 이미 일시정지 중이면 400에러
- 재시작 버튼 → `paused_at` NULL로 초기화. 이미 일시정지 상태가 아니면 400에러 
- **완주 인증 (GPS 검증)**: 유저 좌표와 코스 시작/종료점(`courses.start_lat/lng`, `end_lat/lng`)을 하버사인 공식으로 비교
  - 허용 반경: 기본 **300m** (`config` 상수, 코스 담당이 조정 가능)
  - **정방향**: 유저 시작 ≈ 코스 시작 AND 유저 종료 ≈ 코스 종료
  - **역방향**: 유저 시작 ≈ 코스 종료 AND 유저 종료 ≈ 코스 시작 (해안 트레일 양방향 주행 흔함)
  - 정/역방향 중 하나라도 만족하면 완주. 둘 다 불만족(중도하차 등) 시 `is_completed = false`
  - 검증 기준점은 DB에 저장된 좌표만 사용 (런타임에 두루누비 API 미호출 → API 장애와 무관하게 완주 검증 동작)
- **GPS 권한 필수**: 위치 권한 거부 시 러닝 기록 시작 불가 (회원 전부 해당). 완주 검증이 GPS 기반이므로 좌표 없이는 시작 자체를 막음
- **시간 가드**: `duration_seconds` 60초 미만은 완주 미처리(하한), 24시간 초과는 비정상 기록으로 400 Bad Request(상한, 종료 깜빡 방어)
- 거리는 DRNB/CUSTOM 모두 `courses.distance`(km) 사용 (DRNB도 시드 스크립트가 저장)
- `is_completed = true`인 기록이 있는 유저만 해당 코스 리뷰 작성 가능
- DRNB 코스와 커스텀 코스 모두 기록 가능
- 같은 코스 기록은 횟수 제한 없이 누적 가능 (리뷰 1개 제한과 별개). 마이페이지에서 히스토리로 조회
- `pace` 계산 시 코스 거리가 0이면 에러 방지를 위해 서비스 레이어에서 방어 로직 필요

> v1 한계: 시작/종료 두 지점만 검증하므로 코스 전체 경로 주행은 보장하지 않음 (양 끝점에 직접 가야 하므로 단순 어뷰징은 차단됨). 실시간 경로 추적은 v2에서 도입 예정

---

### 5. 리뷰

| 기능 | 설명 |
|------|------|
| 리뷰 작성 | 완주한 코스에 한해 작성 가능. 리뷰 내용 + 체감 난이도(`EASY`/`NORMAL`/`HARD`) 입력 |
| 리뷰 수정 | 본인만 수정 가능 |
| 리뷰 삭제 | 본인 또는 관리자 삭제 가능 |
| 리뷰 이미지 첨부 | Cloudflare R2 업로드. 최대 5장, 장당 2MB (`REVIEW_IMAGE_MAX_COUNT=5`, `REVIEW_IMAGE_MAX_SIZE_MB=2`) |
| 리뷰 이미지 삭제 | 개별 이미지 삭제 가능. R2에서도 함께 제거 |
| 리뷰 목록 조회 | 코스 상세 페이지에서 해당 코스 리뷰 목록 조회 (최신순, offset 페이지네이션 `?page=1&size=20`) |
| 체감 난이도 평균 표시 | 코스 상세 페이지에서 전체 유저 체감 난이도 평균 표시. 탈퇴 유저 리뷰도 평균 계산에 포함 |
| AI 리뷰 요약 | 코스 리뷰가 3개 이상이면 Gemini가 리뷰를 요약해 코스 상세 페이지에 표시 |

**비즈니스 로직**
- 코스당 리뷰 1개 제한 (UNIQUE INDEX on course_id + user_id). 같은 코스 여러 번 달려도 리뷰는 1개만 작성 가능 (수정은 가능)
- 탈퇴 유저의 리뷰는 화면에 노출하지 않음 (`WHERE user_id IS NOT NULL` 필터링)
- 탈퇴 유저의 리뷰 평점은 체감 난이도 평균 계산에 포함 (통계 목적)
- 관리자 대시보드 "총 리뷰 수" 집계 시 탈퇴 유저 리뷰(`user_id IS NULL`)도 포함 (실제 작성된 리뷰이므로)
- 관리자 삭제 사유: 욕설, 부적절한 내용, 서비스에 악영향을 주는 리뷰

**AI 리뷰 요약 (Gemini)**
- 리뷰가 **3개에 도달하면** 첫 요약 생성. 3개 미만이면 요약 없이 리뷰 원문만 표시
- 이후 새 리뷰가 **5개 추가될 때마다** 재생성 (3 → 8 → 13 ...). 현재 리뷰 수와 `review_summaries.review_count`의 차이가 5 이상이면 갱신
- 요약 결과는 `review_summaries` 테이블에 저장 (코스당 1개, `course_id` UNIQUE). 매 조회마다 Gemini를 호출하지 않고 저장된 요약을 반환
- 요약 대상은 **전체 리뷰** (탈퇴 유저 리뷰 포함 — 난이도 평균과 동일하게 통계성 집계로 취급. 요약문에는 작성자 정보가 들어가지 않음)
- 모델: Gemini 2.5 Flash-Lite (개발 시작 시 최신 모델명 재확인). 연동은 `app/clients/gemini.py`
- 담당: 보류

---

### 6. 편의시설

| 기능 | 설명 |
|------|------|
| 편의시설 목록 조회 | 코스 상세 페이지에서 주변 편의시설 지도 표시 |
| 편의시설 상세 조회 | 카카오맵 API 연동으로 상세 정보 표시 (`kakao_place_id`) |
| 편의시설 등록 | 관리자만 가능. 시설 종류, 이름, 주소, 좌표 입력. 연결할 코스 선택 (선택 사항, 복수 선택 가능) |
| 편의시설 수정 | 관리자만 가능 |
| 편의시설 삭제 | 관리자만 가능. `is_active = false` 처리 |

**편의시설 종류 (`facility_type`)**

| 값 | 설명 | 데이터 소스 |
|----|------|------|
| `RESTROOM` | 화장실 | 카카오 로컬 API 키워드 검색("화장실") - 아래 참고 |
| `PARKING` | 주차장 | 카카오 로컬 API 키워드 검색("주차장") - 2026-09-14부터 전환, 아래 참고 |
| `LOCKER` | 보관함 | 대응 공공API 없음 - 관리자 수동 등록만 |
| `OTHERS` | 기타 | 카카오 로컬 API 키워드 검색("편의점") + 관리자 수동 등록 |

**코스 ↔ 편의시설 매칭 (2026-09 결정 - 반경 기반 자동 매칭으로 전환)**
- 코스가 강원 전체로 확장되고 커스텀 코스가 계속 늘어나는 구조라, "관리자가 코스마다 편의시설을 수동으로 매핑"하는 기존 설계는 확장성이 없다고 판단
- 코스 상세 조회 시점에 코스 시작/종료 좌표 반경(`FACILITY_RADIUS_M`, 기본 1000m — 2026-09-14, 2000m였을 때 코스가 밀집한 관광지에서 시설이 30건 넘게 잡혀 지도가 지저분해지는 문제로 축소) 내 활성 편의시설을 자동으로 찾아서 보여줌 (완주 인증 하버사인 거리 계산과 동일한 방식). 대상 시설이 많아질 수 있어 haversine 정밀 계산 전에 위경도 bounding box로 DB에서 후보를 먼저 좁힘
- `course_facility` 매핑 테이블은 이제 필수가 아니라 **반경 기반 자동 매칭의 예외(반경 밖인데 강제로 보여주고 싶거나, 반경 안인데 빼고 싶은 경우)를 관리자가 수동으로 조정**하는 용도로만 사용 (등록/수정 시 코스 선택은 계속 선택 사항)
  - 포함: 시설 등록/수정 시 `course_ids`로 지정 (기존과 동일)
  - **제외 (2026-09-13 추가)**: `PUT /facilities/{facility_id}/courses/{course_id}` (`{"is_excluded": true}`)로 반경 안이어도 특정 코스에서만 숨김. `DELETE /facilities/{facility_id}/courses/{course_id}`로 포함/제외 override 해제(반경 자동 판정으로 복귀)

**화장실/주차장/편의점 자동 시드와 관리자 조치의 충돌 방지 (2026-09-13 결정, 2026-09-14 동작 수정)**
- courses(`is_admin_managed`)와 facilities(`is_admin_edited`) 둘 다 같은 패턴: **비활성화할 때만 잠기고, 다시 활성화하면 잠금도 같이 풀림.** 이름/주소/좌표 같은 단순 필드 수정은 잠그지 않음 - 다음 자동 재검색이 최신 정보로 계속 갱신하게 둠
  - 처음엔(2026-09-13) 편의시설 쪽을 "관리자가 뭐라도 한 번 손대면 무조건 영구 잠금"으로 만들었는데, 풀 방법이 없어서 오타 하나만 고쳐도 그 시설이 영원히 자동 갱신 대상에서 빠지는 문제가 있었음 → 코스와 동일한 대칭 구조로 수정
- `create_facility`(신규 등록, 관리자 전용)는 항상 `is_admin_edited = true`로 시작 (자동 소스가 없는 데이터라 잠글 이유가 있어서라기보다, 등록 즉시 관리자가 검토한 상태로 취급)
- `update_facility`로 **비활성화**(`is_active: false`)하면 `is_admin_edited = true`로 잠기고, 자동 재시드는 `is_admin_edited = false`인 시설만 upsert 대상으로 삼아(`WHERE` 조건) 관리자가 비활성화한 시설을 되살리지 않음 (예: 관리자가 폐쇄 확인 후 비활성화한 주차장이 다음 재시드로 다시 활성화되는 문제 방지). **활성화**(`is_active: true`)로 되돌리면 잠금도 같이 풀림
- DRNB 코스도 마찬가지: 관리자가 admin 페이지에서 **비활성화**하면 `courses.is_admin_managed = true`로 잠겨서, 두루누비 시드가 "API에 다시 나타나면 자동 재활성화"하는 기본 동작을 건너뜀 (코스명 등 다른 필드는 계속 최신화). 관리자가 다시 **활성화**로 되돌리면 이 잠금도 같이 풀림(2026-09-13) - 안 풀면 이후 이 코스가 API에서 일시적으로 빠졌다 다시 나타나도 영원히 자동 재활성화가 안 되는 문제가 있었음

**화장실/주차장/편의점 데이터 - 카카오 검색 기반 (알려진 v1 한계)**
- 행정안전부 공중화장실정보(공공데이터포털)는 2025-02부터 원본 데이터에서 좌표(WGS84 위도/경도) 필드 제공이 중단됨 - 표준데이터/조회서비스(오픈API) 둘 다 영향받음. 주소만 있고 좌표가 없어 지도 마커로 쓸 수 없음
- 대신 카카오 로컬 API의 "키워드로 장소검색"으로 대체 - 좌표와 `kakao_place_id`를 한 번에 얻을 수 있음. `화장실`/`주차장`/`편의점` 세 키워드를 씀 (`RESTROOM`/`PARKING`/`OTHERS`에 각각 대응)
- **주차장은 2026-09-14부터 공공API에서 이 방식으로 전환**: 한국교통안전공단 주차정보 API(승인은 받아둠, `app/scripts/seed_facilities_parking.py`에 코드는 남아있음)가 페이지/시간대와 무관하게 무작위로(~20% 확률) 응답을 못 주는 게 실측으로 확인돼 신뢰할 수 없었음. **운영 서버 스케줄러에는 아예 등록하지 않음**(`app/scheduler.py` 참고) - 그 스크립트는 지우지 않고 `python -m app.scripts.seed_facilities_parking`로 필요할 때 수동 실행만 가능하게 남겨둠 (나중에 그 API가 안정화되면 스케줄러 재등록 검토). 같은 실제 주차장이 카카오/공공API 양쪽에서 각각 발견되면 서로 다른 자연키(`kakao_place_id` vs `external_ref`)라 중복 등록될 수 있음(허용 - 나중에 공공API가 안정되면 정리)
- DRNB 코스는 시드 스크립트가 좌표를 새로 확보할 때, 커스텀 코스는 생성 시점 + 경로 수정으로 시작 좌표가 바뀔 때 호출해 근처 화장실/주차장/편의점을 찾아 `facilities`에 저장 (`sync_nearby_facilities`, `app/domain/facility/service.py`). 세 키워드 검색(카카오 HTTP 호출)은 서로 무관해 `asyncio.gather`로 동시에 실행 - 순차로 하면 코스 생성/수정 응답이 그만큼 느려짐. `course_facility`에는 연결을 안 만듦 - 카카오 검색 반경이 `FACILITY_RADIUS_M`과 같아 발견 시점엔 항상 반경 안이라 자동 매칭으로 충분하고, 명시적 연결을 남기면 코스가 나중에 다른 위치로 이동했을 때 안 맞는 시설이 "반경 밖 강제 포함" 예외로 계속 노출되는 문제가 있었음
- **실측 결과(2026-09-13)**: 속초 해수욕장 같은 유명 관광지는 2km 반경 내 여러 건이 잘 잡히지만, DMZ 평화의 길 같은 외곽 구간은 5km까지 넓혀도 결과가 없는 경우가 있음. 카카오맵에 등록된 POI 기반이라 외곽 지역 커버리지가 떨어지는 건 알려진 v1 한계로 감수하고 진행 - 코스에 따라 결과가 없을 수 있음
- 카카오 API 실패/결과 없음/DB 오류는 모두 조용히 스킵 - 코스 생성/시드 자체를 막지 않음. 세 카테고리 중 하나가 실패해도 나머지는 그대로 반영 (`sync_nearby_facilities` 내부에서 전부 처리하고 예외를 호출부로 전파하지 않음)
- **실서비스 규모 확장 시 재검토 필요 (2026-09-14, 지금은 공모전 스코프라 보류)**: 지금은 코스 생성/수정 요청이 카카오 검색(최대 6회 호출)을 끝까지 기다렸다가 응답하는 동기 구조라, 코스가 많아지면 저장 응답 지연이 커짐 - 응답 후 백그라운드로 분리 필요(`BackgroundTasks` 또는 별도 워커/큐). 또한 지금은 "코스 단위"로 동기화가 걸려서 코스가 밀집한 지역은 거의 같은 결과를 코스 수만큼 반복 검색하게 됨(카카오 쿼터 낭비) - 코스가 아니라 지역(격자) 단위로 "최근에 이미 훑었는지" 캐싱하는 구조로 바꾸는 게 나음. 시설 수가 전국 규모로 늘면 지금의 bounding box + Python haversine 방식 대신 PostGIS 같은 공간 인덱스 기반으로 리팩토링도 검토.
- **데이터 신선도 문제**: 재검색 트리거가 "코스 좌표가 바뀔 때"뿐이라, 좌표를 안 건드리는 코스(두루누비 대부분)는 한 번 저장된 이후 영영 재검색되지 않음 - 근처 다른 코스가 우연히 갱신되지 않는 한 폐업/신규 개업 등이 반영 안 됨. 해결 방향 두 가지 검토: ① 지금 방식(이벤트 트리거) 위에 주기적 재검증 배치를 하나 더 얹기(예: 월 1회, 코스 좌표 변경 여부와 무관하게 강제 재검색 - 꺼둔 주차장 공공API 배치가 하던 역할을 카카오 기반으로 대체), ② 아예 다른 방식(예: 코스 상세 조회 시점마다 API를 실시간 호출)으로 신선도 문제 자체를 구조적으로 해결 - 단, 관광지 추천(3-5)이 2026-09부로 이 방식으로 바뀐 건 신선도 때문이 아니라 공모전 규정(로컬 캐싱 금지) 때문이고, 캐싱이 없어진 대신 API 쿼터 소진 위험을 전역 카운터로 따로 관리해야 했음(3-5 참고) - 편의시설은 관리자 CRUD/잠금/코스 간 공유가 필요해 그 방식을 그대로 가져오기 어려움 - ①이 더 유력하지만 실제 설계 시 재검토.
- **코스 하나의 반경 내 편의시설이 100건(백엔드 페이지 상한)을 넘으면 "가까운 순" 표시가 깨질 수 있음 (2026-09-15, 코드리뷰 지적 - 지금은 실제로 발생하지 않아 보류)**: `get_facilities(course_id=...)`(`app/domain/facility/service.py`)는 반경 내 시설을 거리순이 아니라 `facility_id` 순으로 최대 100건만 반환하는데, 프론트(`CourseDetail.jsx`)는 그 응답 안에서만 "시작/종료 지점 기준 가까운 순 상위 N개"를 골라 보여줌 - 만약 한 코스 반경 내 활성 시설이 100건을 넘으면, 응답 자체가 이미 등록순으로 잘려있어서 실제로 가장 가까운 시설이 애초에 빠져있을 수 있음. 2026-09-15 기준 로컬 DB 실측 결과 활성 코스 41개 중 최댓값이 54건(100건 미만)이라 지금은 보류. 나중에 고칠 때는 `_get_nearby_facility_ids`가 이미 계산해둔 haversine 거리를 기준으로 `get_facilities`가 정렬해서 반환하도록 바꾸는 방향이 유력 - 단, 지금 `total`/`offset` 페이지네이션 구조(SQL 정렬)와 거리 정렬(Python 계산)이 안 맞아 리팩토링이 필요해서 코드 변경 범위가 작지 않음.

**비즈니스 로직**
- `kakao_place_id` 입력 시 `place_url`은 저장 없이 응답 조립 시점에 자동 계산 (컬럼 아님, property)
- `kakao_place_id` 없는 편의시설은 우리 DB 정보(이름, 주소)만 표시
- 코스 상세에서 편의시설 조회 시 `facilities.is_active = true`인 것만 노출. 비활성화된 편의시설은 `course_facility` 매핑이 남아있거나 반경 안이어도 화면에 노출하지 않음

---

### 7. 관리자

| 기능 | 설명 |
|------|------|
| 편의시설 CRUD | 편의시설 등록 / 수정 / 삭제 |
| 코스 관리 | 코스 조회(활성/비활성 무관) + 활성화/비활성화만 가능 (수정 기능은 지원하지 않음, 2026-09 결정) |
| 리뷰 삭제 | 부적절한 리뷰 삭제 (욕설, 부적절한 내용 등) |
| 유저 강제 탈퇴 | 지속적으로 문제가 되는 유저 강제 탈퇴 처리 |
| 밴 목록 조회 | 강제 탈퇴로 재가입이 막힌 소셜 계정 목록 조회 |
| 밴 해제 | 강제 탈퇴로 막힌 소셜 계정의 재가입 차단 해제 |
| 대시보드 통계 조회 | 아래 통계 항목 참고 |

**유저 강제 탈퇴 비즈니스 로직**
- 강제 탈퇴당한 유저가 사용하던 `provider_type` + `provider_uid`를 `banned_accounts`에 기록, 이후 해당 소셜 계정으로는 재가입 불가
- 소셜 계정 연동 기능이 없어(위 회원 섹션 참고) 계정당 소셜 계정은 1개뿐이므로, 밴도 **그 1개 계정만** 막음
- **한계**: 강제 탈퇴당한 사람이 한 번도 쓴 적 없는 다른 프로바이더(예: 네이버로 밴 → 구글로 재가입)로 오면 막을 수 없음. 별도 식별 수단(전화번호 인증 등) 없이는 구조적으로 불가능 — 현재 스코프에서는 이 한계를 감수하기로 함
- 밴 해제는 `banned_accounts`에서 해당 row를 삭제하는 방식 (오탈퇴/재심사 대응용)

**관리자 대시보드 통계 항목**

| 분류 | 항목 |
|------|------|
| 유저 | 총 유저 수, 신규 가입자 수 (일별/주별/월별), 활성 유저 수 (최근 30일), 탈퇴 수 (일별/월별) |
| 러닝 기록 | 총 누적 러닝 거리, 총 완주 횟수, 오늘/이번주/이번달 완주 횟수 |
| 코스 | 전체 인기 코스 TOP 5 (완주 횟수 기준), DRNB 인기 코스 TOP 3, 커스텀 인기 코스 TOP 3, 커스텀 코스 등록 수 |
| 리뷰 | 총 리뷰 수 |
| 편의시설 | 등록된 편의시설 수 (타입별) |

> 탈퇴 수 집계는 `users.deleted_at` 기준  
> 활성 유저는 `users.last_login_at` 기준 최근 30일 내 로그인/토큰 재발급 이력이 있는 유저 (러닝 기록 여부와 무관 — 로그인만 하고 안 달려도 활성으로 집계)  
> ⚠️ `last_login_at` 배포 직후엔 기존 유저 전원이 NULL로 시작 — 재로그인/재발급 전까지 활성 유저 수가 일시적으로 낮게 보일 수 있음 (실제 이탈 아님, 며칠 내 정상화)  
> 인기 코스는 `records.is_completed = true` 기준 완주 횟수로 집계. 동률이면 리뷰 개수가 많은 순으로 2차 정렬

---

## 외부 서비스 연동

| 서비스 | 용도 |
|--------|------|
| 두루누비 API (공공데이터포털) | DRNB 코스 목록/상세정보 배치 조회 (시드 스크립트가 적재, 요청 시점 실시간 호출 없음) |
| 카카오맵 API | 편의시설 지도 표시, 코스 경로 표시, 장소 상세정보 연동, 커스텀 코스 폼 주소/장소 검색. 카카오 로컬 API 키워드 검색으로 화장실/주차장/편의점 편의시설 데이터 자체도 채움(섹션 6 참고) |
| Google / Kakao / Naver OAuth | 소셜 로그인 |
| Cloudflare R2 | 프로필 이미지, 리뷰 이미지, 커스텀 코스 이미지 저장 |
| Gemini API (Google) | AI 리뷰 요약 생성 (리뷰 3개 이상 코스), AI 코스 날씨·안전 브리핑 (2.5 Flash-Lite 모델 공용) |
| 기상청 단기예보/기상특보 조회서비스 (공공데이터포털) | AI 코스 날씨·안전 브리핑용 원본 데이터 |
| 한국관광공사 국문 관광정보 서비스 (공공데이터포털) | 코스 시작/종료점 주변 관광지 추천 카드 |
| Redis | Access Token 블랙리스트, Refresh Token 저장, OAuth state 저장, AI 날씨·안전 브리핑 캐싱 |

> **data.go.kr 활용신청 승인 목록 (2026-08-27 기준, 8개 — 사용 4개, 미사용 4개)**
> ✓ 한국관광공사 두루누비 정보 / ✓ 한국관광공사 국문 관광정보 / 한국관광공사 관광사진 정보(미사용) / 기상청 관광코스별 관광지 상세 날씨 조회서비스(미사용, 좌표기반 API로 대체 결정) / 행정안전부 공중화장실정보 조회(미사용 - 2025-02부터 좌표 필드 제공 중단으로 카카오 검색 대체) / 한국교통안전공단 주차정보(운영 미사용 - 응답 불안정으로 카카오 검색 대체, 섹션 6 참고) / ✓ 기상청 단기예보 조회 / ✓ 기상청 기상특보 조회

---

## 미결정 사항

| 항목 | 내용 |
|------|------|
| ~~두루누비 코스 이미지~~ | (해결, 2026-08-02) `courseList` 응답 필드 실측 확인 결과 이미지 필드 없음. DRNB 코스 이미지는 API 미제공 — 일단 `course_images`는 CUSTOM 코스 전용 유지 |
| ~~편의시설 데이터 소스~~ | (해결, 2026-09-13) 주차장은 한국교통안전공단 API 배치 시드, 화장실은 좌표 미제공 문제로 카카오 로컬 API 키워드 검색 대체, 보관함/기타는 관리자 수동 등록. 코스-편의시설 매칭도 관리자 수동 매핑 대신 반경 기반 자동 매칭으로 전환 (섹션 6 참고) |
